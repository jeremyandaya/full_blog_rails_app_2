<h1>Edit Reply</h1>
<%= form_for([@post, @comment]) do |f| %>
	<%= f.label :comment, "Comment" %><br>
	<%= f.text_area :comment %><br>
	<%= f.submit class: 'myButton' %><br><br>
	<%= link_to "Cancel", post_path(@post), class: "myButton" %>
<% end %>